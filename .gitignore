# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# P8：本機開發用的假密鑰/密碼（wrangler pages dev 自動讀取），不可進版控
.dev.vars
.wrangler

# main 是通用範本，真實行程的 D1 備份/種子檔與設計稿只留在各行程分支（見 README）
/backups
/seed.sql
/design

# trip-cli：每趟行程的本機素材（wrangler.toml/trip.conf/favicon/hero-photo 等）
# 跟本機快取的 VAPID 金鑰，刻意不進版控——見 scripts/trip-cli/README.md
/local-trips
/.trip-cli-cache

# worker-cron 是帳號層級共用的 Worker，不是行程層級，所以設定檔另外用 profile
# 分（不是 trip），跟 local-trips 同樣的理由不進版控——見 worker-cron/wrangler.toml
# 開頭註解、scripts/trip-cli/README.md
/local-worker-cron
